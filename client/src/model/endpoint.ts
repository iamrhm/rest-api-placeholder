export interface ClientEndpointDetails{
  endpoint: string, status: number, response: string, method: string 
}

export interface ServerEndpointDetails {
  endpoint: string, status: number, response: object, method: string
}